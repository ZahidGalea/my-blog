---
import BlogSearch from './BlogSearch';

interface Props {
  allPosts?: any[];
}

const { allPosts = [] } = Astro.props;
const posts = allPosts.filter((post: any) => !post.frontmatter?.draft && post.frontmatter?.title);
---

<BlogSearch client:load searchList={posts as any}/>